export enum UserCharacter {
  ADMIN = "admin",
  MODERATOR = "moder",
  COMMON = "common",
}

export enum UserStatus {
  Active = "active",
  Pending = "pending",
  Banned = "banned",
  Deleted = "deleted",
}
